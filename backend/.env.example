# Copy to .env and fill in.
GEMINI_API_KEY=your-gemini-api-key
# LIVE_MODEL=gemini-3.1-flash-live-preview   # override the Live model if needed
# USE_LLM=1                                  # route replies through the ADK LlmAgent (default: return script directly)
